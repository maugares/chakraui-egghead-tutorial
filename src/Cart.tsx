import { VStack } from '@chakra-ui/react'

const Cart = () => (
    <VStack
      width='full'
      height='full'
      padding={10}
      spacing={10}
      alignItems='flex-start'
      background='lightblue'
    />
  )

export default Cart
