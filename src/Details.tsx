import { VStack } from '@chakra-ui/react'

const Details = () => (
    <VStack
      width='full'
      height='full'
      padding={10}
      spacing={10}
      alignItems='flex-start'
      background='pink'
    />
  )

export default Details
